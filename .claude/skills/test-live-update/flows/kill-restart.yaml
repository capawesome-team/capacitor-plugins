appId: com.example.plugin
name: Force-kill, restart and assert the title
---
- stopApp
- launchApp
- extendedWaitUntil:
    visible: "${typeof TITLE !== 'undefined' ? TITLE : 'Capacitor Live Update v2'}"
    timeout: 30000
