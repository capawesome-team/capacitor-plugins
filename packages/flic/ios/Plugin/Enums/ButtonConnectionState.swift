import Foundation

public enum ButtonConnectionState: String {
    case connected = "CONNECTED"
    case connecting = "CONNECTING"
    case disconnected = "DISCONNECTED"
    case disconnecting = "DISCONNECTING"
}
