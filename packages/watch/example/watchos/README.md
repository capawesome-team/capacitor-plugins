# watchOS Example App

This folder contains the source files for a minimal watchOS companion app.
Since watchOS apps require an Xcode target, the native project is not committed.

Follow the watchOS setup guide in the [plugin README](../../README.md) to create
the watch app target in your Xcode project and then add these source files to it.
